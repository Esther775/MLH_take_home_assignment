@array.map do |commit|
  {
    contributors: [
      github_username: commit["author"]["login"],
      message: commit["commit"]["message"],
      email: commit["commit"]["author"]["email"],
    # date:
    ],
  }
end
